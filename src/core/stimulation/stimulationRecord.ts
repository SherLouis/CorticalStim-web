import StimulationFormValues from "../models/stimulationForm";

// TODO: unsure how to use this yet ...
export default interface StimulationRecord {
    id: string;
    stimulation_data: StimulationFormValues;
}